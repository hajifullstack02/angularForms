import { HighligthColorDirective } from './highligth-color.directive';

describe('HighligthColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligthColorDirective();
    expect(directive).toBeTruthy();
  });
});
